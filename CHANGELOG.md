# 更新日志

## 0.10.1 - 2026-07-20

### 功能

- 新增「仅监控」模式：发现余量时提醒但不自动提交。
- 监控列表支持 ↑↓ 调整优先级（越靠前越先检查）。

### 工程

- 拆分 `worker`（state/time/runner）与 `app`（theme）模块。
- 日志缓冲改为 `VecDeque`。
- 登录参数封装为 `LoginRequest`。
- 深色主题对比度完善。
- `package.ps1` 默认跑测试并生成 SHA256/BUILD_INFO；`audit.ps1` 检测任意 quick-xml；新增 `clean-privacy.ps1`。

### 稳定性与正确性

- 修复停止后立即重新开始时，旧任务退出可能把新任务误判为停止的竞态。
- 停止过程中保持“正在停止”，后台真正退出前不能抢占启动新任务。
- “结果通知”与“提示音”开关拆分生效；会话保活提示也遵循这两项设置。
- 提示音改为直接调用系统 Beep，不再每次拉起 PowerShell。
- 日志中的账号改为脱敏显示；登录密码哈希改为分片写入，避免中间明文 String。
- HTTP 响应改为流式限长，超大 chunked 响应会在读满内存前中止。
- 登录相关重定向限制为同源，降低跨域跳转风险。
- 默认定时开抢时间改为“今天 08:00”，不再写死过期日期。
- 清理 runtime/debug 下可能含个人信息的调试页面残留。
- 识别 HTTP 429/503 的 Retry-After，抢课退避优先遵循服务器等待时间。
- 人数接口失败时仍返回课程列表，余量标记为未知，避免误抢；限流会向上传递。
- 引入强类型余量状态 `SeatInfo`：缺少 limit 或 0 占位不再被当成“已满/有余量”。
- 选课“成功”后增加轻量二次确认（课程是否仍在可选列表）。
- 将 `eams` 拆为 `types` / `parse` / 客户端模块，便于后续测试与维护。
- 统一 `rust-version` 与工具链为 1.97.1。


### 界面与体验

- 定时开抢改为独立卡片布局，避免一行挤爆被截断。
- 支持年月日时分秒滚轮选择，并提供「设为现在」「明天 08:00」快捷按钮。
- 显示当前设定、本地现在时间，以及倒计时/已过期提示；旧版 HH:MM 配置自动升级。

## 0.10.0 - 2026-07-20

### 功能增强

- 开始抢课前确认清单：展示监控目标、间隔与开关项，并通过开抢自检。
- 监控结果整理：成功目标沉底显示；可一键清理失败/成功目标；卡片显示上次检查时间。
- 定时开抢：高级设置中指定 HH:MM，到点且已登录时自动开始。
- 本轮结束后弹出结果摘要（成功/失败/停止与明细）。

## 0.9.0 - 2026-07-20

### 功能增强

- 监控卡片显示课程名、教师、余量与检查次数。
- 抢课成功/失败应用内通知与系统提示音；登录失效自动提醒。
- 限流自适应间隔：遇到过快/限流自动拉长，恢复后逐渐回到设定值。
- 会话保活：登录空闲约 3 分钟轻量刷新，发现过期会提示重新登录。
- 课程列表支持余量优先/名称排序、有余量高亮、搜索与「仅有余量」记忆。
- 间隔预设（稳妥/均衡/激进）、优先检查有余量、结果通知与提示音开关。
- 日志分级筛选与导出；配置导入/导出；打开数据目录与崩溃日志位置。
- 首次使用引导、危险操作确认（退出登录/清空日志/移除监控）。
- 界面缩放与深色模式。


## 0.8.1 - 2026-07-19

### 稳定性修复

- 修复顶部右对齐状态区在部分布局时产生无穷宽度、导致 egui 触发 panic 并退出的问题。
- 自定义开关改用标准布局分配，不再手动移动光标。
- 增加非有限尺寸防御和右对齐布局回归测试。
- 增加本地崩溃报告，异常退出时写入 `%APPDATA%\CourseMonitor\crash.log`，便于后续排障。

## 0.8.0 - 2026-07-19

### 安全与正确性

- 发布内容改为白名单，不再包含真实配置、调试页面、EXE 或源码压缩包。
- 配置迁移到 `%APPDATA%\CourseMonitor`，采用临时文件、备份和恢复式保存。
- 密码不落盘，后台使用自动清零内存包装，登录成功和退出时清理界面密码。
- 教务地址默认强制 HTTPS，仅允许 localhost 使用 HTTP。
- 自动选课改为严格课程序号匹配；多条匹配会停止目标，避免选错教学班。
- 修复中文 HTML 字节切片可能导致的崩溃。
- 修复失败响应包含“成功”字样时可能被误判为成功的问题。
- 登录失效、HTTP 状态和过大响应现在使用统一错误分类。

### 稳定性

- 后台任务改为共享 Tokio runtime，增加活动守卫和可取消运行代次。
- 连续网络错误和提交错误达到阈值后自动停止。
- 轮询加入随机抖动，网络错误使用指数退避。
- 区分人数未知和课程已满，不再把 `0/0` 显示为已满。
- 运行期间锁定连接、轮次、间隔和监控目标。
- 结果汇总区分成功与失败/跳过，不再把跳过报告为全部完成。

### 界面体验

- 重构顶部控制区，将账号连接、课程刷新和自动抢课分组展示。
- 统一字号、控件高度、卡片圆角、边距和表格行高，提升对齐与信息层级。
- 使用低饱和自然色、半透明雾面卡片和细高光边框，形成克制的水晶毛玻璃视觉。
- 增加设置面板、日志抽屉、状态提示和悬停反馈的平滑过渡动画。
- Windows 11 启用系统 Mica 背景与圆角，旧版 Windows 自动回退到应用内雾面效果。
- 优化监控目标空状态、课程筛选区、课程表格和长日志的阅读体验。
### 工程化

- 升级 eframe/egui、reqwest、scraper 和 toml。
- 增加 Windows CI、发布产物上传和安全打包脚本。
- 增加配置、解析、HTTP、登录哈希和 Cookie 会话测试。
- 格式和 Clippy 警告纳入 CI 强制检查。


